[Unit]
Description=MonitoringBot
After=syslog.target
After=network.target

[Service]
Type=simple
User={{ ansible_ssh_user }}
Group={{ ansible_ssh_user }}
ExecStart=/usr/bin/python3 /home/{{ ansible_ssh_user }}/bot/monitor_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
